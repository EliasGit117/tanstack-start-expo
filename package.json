{
  "name": "tanstack-rnw-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web": "cd apps/web && bun run dev",
    "expo": "cd apps/expo && bun run start",
    "ios": "cd apps/expo && bun run ios",
    "android": "cd apps/expo && bun run android",
    "build:web": "cd apps/web && bun run build",
    "test": "bun --filter '*' test"
  },
  "trustedDependencies": [
    "esbuild",
    "lightningcss"
  ]
}
